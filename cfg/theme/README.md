## Theme Files

Reserved for system theme assets (GTK/Qt themes, icon sets, cursor themes, and the scripts that apply them).

Currently empty. Theme work is tracked against the Niri and LXQt paths; do not add ad-hoc theme files here until the apply script and config layout are defined.
